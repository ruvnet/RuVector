[package]
name = "mcp-gate"
version = "0.1.0"
edition = "2021"
description = "MCP (Model Context Protocol) server for the Anytime-Valid Coherence Gate"
license = "MIT OR Apache-2.0"
repository = "https://github.com/ruvector/ruvector"
keywords = ["mcp", "coherence", "gate", "agent", "permission"]
categories = ["network-programming", "asynchronous"]

[lib]

[[bin]]
name = "mcp-gate"
path = "src/main.rs"

[[bin]]
name = "schema-cache-bench"
path = "src/bin/schema_cache_bench.rs"

[features]
default = []

[dependencies]
cognitum-gate-tilezero = { version = "0.1.1", path = "../cognitum-gate-tilezero" }
# Reuses the WP28/ADR-331 value-of-information primitive rather than
# restating its mathematics; see src/monitor/mod.rs.
ruvector-tiny-dancer-core = { version = "2.3.0", path = "../ruvector-tiny-dancer-core" }
async-trait = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.35", features = ["full"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
thiserror = "1.0"
hex = "0.4"
sha2 = "0.10"
base64 = "0.21"
futures = "0.3"

[dev-dependencies]
tokio = { version = "1.35", features = ["rt-multi-thread", "macros", "test-util"] }
