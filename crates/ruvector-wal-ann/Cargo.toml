[package]
name = "ruvector-wal-ann"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Streaming Write-Ahead Log for ANN with Coherence-Gated Merge: buffer inserts in a WAL, serve WAL via linear scan, merge into navigable graph when a coherence gate fires"
keywords = ["vector-search", "ann", "wal", "agent-memory", "streaming"]
categories = ["algorithms", "data-structures", "science"]

[[bin]]
name = "benchmark"
path = "src/bin/benchmark.rs"

[dependencies]
rand       = { workspace = true }
rand_distr = { workspace = true }
thiserror  = { workspace = true }

[lints.rust]
dead_code         = "allow"
unused_variables  = "allow"
