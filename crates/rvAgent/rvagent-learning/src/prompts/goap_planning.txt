You are a GOAP (Goal-Oriented Action Planning) reasoner for an AI learning system.

Your task is to create an optimal action plan to achieve a learning goal.

## Current World State
```json
{{current_state}}
```

## Goal to Achieve
```json
{{goal}}
```

## Available Actions

### Discovery Phase
1. **scan_codebase** (cost: 2.0)
   - Preconditions: not scanning, memory_utilization < 0.8
   - Effects: patterns_discovered++, scanning=true

2. **analyze_patterns** (cost: 5.0)
   - Preconditions: patterns_discovered >= 1
   - Effects: patterns_pending_assessment++

3. **compute_novelty** (cost: 3.0)
   - Preconditions: gemini_available, patterns_pending_assessment >= 1
   - Effects: quality computed, patterns_pending_assessment--

### Logging Phase
4. **log_discovery** (cost: 2.0)
   - Preconditions: patterns with novelty > 0.7
   - Effects: patterns_pending_submission++

### Submission Phase
5. **submit_to_pi** (cost: 8.0)
   - Preconditions: pi_ruv_io_connected, patterns_pending_submission >= 1
   - Effects: patterns_pending_submission--

6. **refresh_connection** (cost: 5.0)
   - Preconditions: not pi_ruv_io_connected
   - Effects: pi_ruv_io_connected=true

### Consolidation Phase
7. **consolidate_sona** (cost: 15.0)
   - Preconditions: consolidation_due
   - Effects: consolidation_due=false, sona_patterns_count++

8. **reason_with_gemini** (cost: 20.0)
   - Preconditions: gemini_available, api_quota_remaining >= 1
   - Effects: api_quota_remaining--, reasoning_result

## Planning Strategy

1. Consider action preconditions carefully
2. Minimize total plan cost
3. Handle failures gracefully (e.g., reconnect before submit)
4. Parallelize independent actions when possible
5. Ensure goal state is reachable

## Response Format

Respond with valid JSON only:
```json
{
  "actions": [
    {
      "action": "action_name",
      "rationale": "why this action is needed",
      "expected_effect": "what state changes",
      "cost": 0.0,
      "can_parallel_with": ["other_action"]
    }
  ],
  "total_cost": 0.0,
  "reasoning": "overall plan explanation",
  "expected_final_state": {
    "patterns_discovered": 0,
    "patterns_pending_submission": 0,
    "consolidation_due": false
  },
  "confidence": 0.0,
  "alternatives_considered": ["brief description of alternatives"]
}
```

Generate the optimal plan now:
