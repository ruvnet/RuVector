{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permission set for the reader window. The file dialog is the only host capability granted: it is how the user chooses which .rvf to open. No filesystem read, shell, HTTP, or asset-protocol permission is granted — the reader's own Rust commands are the only path to the filesystem, and they read metadata only.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
